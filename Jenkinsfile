pipeline {
    agent any

    stages {
        stage('Build Docker Image') {
            steps {
                    sh "pwd"
                    sh "ls -l"
            }
        }

        stage('Clone Code') {
            steps {
                echo "Hello"
            }
        }
    }
}
